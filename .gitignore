# Secrets — never commit. The negation keeps the template file tracked.
.env
.env.*
!.env.example

# Vector index. Contains knowledge-base content verbatim, and this repo is
# public. Rebuildable from the source documents, so nothing is lost.
.chroma/

# Real knowledge-base documents are internal company material and this repo
# is public. Committed examples live in examples/sample_kb instead.
knowledge_base/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# Tooling
.pytest_cache/
.mypy_cache/
.idea/
